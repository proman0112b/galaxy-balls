/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // swcMinify: true,
  // i18n: {
  //   locales: ['default', 'en', 'fr', 'es', 'de', 'pt', 'it'],
  //   defaultLocale: 'default',
  // },
}

module.exports = nextConfig
